pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello as well' // Echo a greeting
                echo 'Printing path ${WORKSPACE}'
                // Cat the contents of testing.txt in the workspace
                cat "type ${WORKSPACE}\\kube\\testing.txt"
            }
        }
    }
}
